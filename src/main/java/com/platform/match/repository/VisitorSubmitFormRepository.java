package com.platform.match.repository;

import com.platform.match.domain.VisitorSubmitForm;
import org.springframework.data.jpa.repository.JpaRepository;

public interface VisitorSubmitFormRepository extends JpaRepository<VisitorSubmitForm, Long> {
}